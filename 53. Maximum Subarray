class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int c=0; 
        int ans = -2147483648;
        for(int i=0;i<nums.size();i++)
        {
            c = max(nums[i],c+nums[i]);
            ans = max(c,ans);
        }
        return ans;
    }
};
